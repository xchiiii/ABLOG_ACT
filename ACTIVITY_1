DROP DATABASE gyt;
CREATE DATABASE gyt;
use gyt;

CREATE TABLE client (
	client_id int PRIMARY KEY AUTO_INCREMENT,
    fname varchar(255),
    lname varchar(255),
    gender ENUM ('female','male') DEFAULT 'male',
    age int,
    contact_add varchar(255),
    email varchar(255),
    password varchar(255)

);

CREATE TABLE transaction_records(
	trans_id INT PRIMARY KEY AUTO_INCREMENt,
    transname varchar(255),
    client_id int,
    amount int,
    date date,
    CONSTRAINT FKclientID FOREIGN KEY (client_id)
    REFERENCES client(client_id)


);

CREATE TABLE reports(
	report_id INT PRIMARY KEY AUTO_INCREMENT,
    total_amount varchar (255),
    date date,
    client_id int,
    trans_id int,    
	CONSTRAINT FKreportclientID FOREIGN KEY (client_id)
    REFERENCES client(client_id),
	CONSTRAINT FKtransID FOREIGN KEY (trans_id)
    REFERENCES transaction_records(trans_id)

);
CREATE TABLE trainor (
    trainor_id INT PRIMARY KEY AUTO_INCREMENT,
    name varchar (255),
    salary int,
    email varchar (255),
    password varchar (255)
   
);


CREATE TABLE schedule(
    sched_id INT PRIMARY KEY AUTO_INCREMENT,
    client_id int,
    trainor_id int,
    session varchar (255),
    activity varchar (255),
    date date,
    time_start time,
    time_end time, 
    CONSTRAINT FKschedclientID FOREIGN KEY (client_id)
    REFERENCES client (client_id),
    CONSTRAINT FKtrainorID FOREIGN KEY (trainor_id)
    REFERENCES trainor (trainor_id)
);


CREATE TABLE payment(
    payment_id INT PRIMARY KEY AUTO_INCREMENT,
    client_id int,
    date date,
    amount int,
    CONSTRAINT FKpayclID FOREIGN KEY (client_id)
    REFERENCES client (client_id)

);

CREATE TABLE membership(
    mem_id INT PRIMARY KEY AUTO_INCREMENT,
    client_id int,
    status varchar (255),
    date date,
    CONSTRAINT FKmemID FOREIGN KEY (client_id)
    REFERENCES client (client_id)
);


INSERT INTO client (client_id, fname, lname)  VALUES ( 1, 'Jay Vhine', 'Ablog'),( 2, 'Jasmine', 'Ablog'),(3, 'Jet', 'Ablog') ;
INSERT INTO schedule(date, time_start, session, activity )  VALUES ('03-02-01', '3:00 pm', '1','swimming'), ('03-03-04', '4:00 pm',  '2', 'running'), ('04-02-06', '5:00 pm', '3', 'biking');
INSERT INTO trainor (trainor_id, name)  VALUES (1,'Blas'),(2,'Ablog'),(3,'Chen');
INSERT INTO reports( total_amount)  VALUES ('3,000php'),('4,000php'),('5,000php');
INSERT INTO payment  (client_id, amount)   VALUES(1,7000),(2,8000),(3,9000);
INSERT INTO membership (client_id, status)  VALUES(1,'Active'),(2,'Inactive'),(3,'Active');

